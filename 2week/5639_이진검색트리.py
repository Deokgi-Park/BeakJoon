pre = []
while True:
    try:
        pre.append(int(input()))
    except:
        break
left = []
right = []
N = len(pre)
root = pre[0]


def node(node, i):
    if root >= node :
        



node(root,0)
node(root,0)

print(root)